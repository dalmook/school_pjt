version: "3.9"

services:
  web:
    build: .
    container_name: neis-school-alert
    ports:
      - "18083:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
